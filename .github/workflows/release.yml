# OpenCCU Release build
# yamllint disable rule:truthy
---
name: Release Build

on:
  workflow_dispatch:
    inputs:
      release_date:
        description: 'Release date override (YYYYMMDD)'
        required: true
        type: string
        default: 'YYYYMMDD'
      test_workflow:
        description: 'Test workflow (dry-run)?'
        required: true
        type: 'boolean'
        default: 'false'
      platforms:
        description: 'List of platforms to build (comma seperated, e.g. "rpi3","rpi4")'
        required: true
        default: 'all'
        type: string

# default read-only permission
permissions:
  contents: read

jobs:
  release_draft:
    permissions:
      contents: write  # ncipollo/release-action
      pull-requests: read  # mikepenz/release-changelog-builder-action
    name: Release draft
    runs-on: ubuntu-24.04
    outputs:
      upload_url: ${{ steps.release_drafter.outputs.upload_url }}
      occu_version: ${{ steps.env.outputs.occu_version }}
      version: ${{ steps.env.outputs.version }}
      date: ${{ steps.env.outputs.date }}
      tag: ${{ steps.env.outputs.tag }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup Environment
        id: env
        shell: bash
        run: |
          if [[ "${{ github.event.inputs.release_date }}" == "YYYYMMDD" ]]; then
            BUILD_DATE=$(date +%Y%m%d)
          else
            BUILD_DATE=${{ github.event.inputs.release_date }}
          fi
          OCCU_VERSION=$(grep 'OCCU_VERSION =' buildroot-external/package/occu/occu.mk | cut -d' ' -f3 | cut -d'-' -f1)
          echo "occu_version=${OCCU_VERSION}" >> $GITHUB_OUTPUT
          echo "version=${OCCU_VERSION}.${BUILD_DATE}" >> $GITHUB_OUTPUT
          echo "date=${BUILD_DATE}" >> $GITHUB_OUTPUT
          if [[ "${{ github.event.inputs.test_workflow }}" == "true" ]]; then
            echo "tag=${OCCU_VERSION}.${BUILD_DATE}-draft" >> $GITHUB_OUTPUT
          else
            echo "tag=${OCCU_VERSION}.${BUILD_DATE}" >> $GITHUB_OUTPUT
          fi

      - name: Get previous tag
        id: previoustag
        uses: WyriHaximus/github-action-get-previous-tag@61819f33034117e6c686e6a31dba995a85afc9de  # v2.0

      - name: Generate changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@cb021f9b36a51a7c6f18e4679b6fa2cb77a1260c  # v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          configuration: ".github/release-changelog-config.json"
          fromTag: ${{ steps.previoustag.outputs.tag }}
          toTag: ${{ github.sha }}

      - name: Generate release notes
        shell: bash
        run: |
          export CHANGELOG="$(cat <<'EOF' | sed 's/@Copilot/@jens-maus/g'
          ${{ steps.changelog.outputs.changelog }}
          EOF
          )"
          export VERSION=${{ steps.env.outputs.version }}
          export PREVIOUS_TAG=${{ steps.previoustag.outputs.tag }}
          envsubst <.github/release-template.md >/tmp/release-template.md

      - name: Generate ChangeLog summary
        shell: bash
        run: |
          CHANGE_ITEMS=$(cat <<'EOF' | sed -n 's/^[[:space:]]*[-*][[:space:]]\+//p' | sed 's/@Copilot/@jens-maus/g'
          ${{ steps.changelog.outputs.changelog }}
          EOF
          )

          mkdir -p /tmp/changelog-summary

          generate_summary() {
            local lang="$1"
            local outfile="$2"
            local label_fallback
            case "${lang}" in
              de)
                label_fallback='Siehe Release-Seite f&uuml;r den vollst&auml;ndigen Changelog.'
                ;;
              *)
                label_fallback='See release page for the full changelog.'
                ;;
            esac
            {
              echo '<ul>'
              if [[ -n "${CHANGE_ITEMS}" ]]; then
                printf '%s\n' "${CHANGE_ITEMS}" | python3 -c "import html,sys; [print('<li>' + html.escape(line.strip(), quote=True).encode('ascii', 'xmlcharrefreplace').decode('ascii') + '</li>') for line in sys.stdin if line.strip()]"
              else
                echo "<li>${label_fallback}</li>"
              fi
              echo '</ul>'
              echo '<hr/>'
            } >"${outfile}"
          }

          generate_summary en /tmp/changelog-summary/changelog-summary.en.html
          generate_summary de /tmp/changelog-summary/changelog-summary.de.html

      - name: Create release draft
        id: release_drafter
        uses: ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd  # v1.21.0
        with:
          tag: ${{ steps.env.outputs.tag }}
          name: 'OpenCCU ${{ steps.env.outputs.version }}'
          bodyFile: /tmp/release-template.md
          draft: true
          prerelease: false
          allowUpdates: true
          token: ${{ secrets.RELEASE_USER_PAT }}

      - name: Upload release-template.md artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          path: /tmp/release-template.md
          name: release-template.md

      - name: Upload ChangeLog summary artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          path: /tmp/changelog-summary/
          name: changelog-summary

  build:
    permissions:
      contents: write      # actions/upload-artifact, shogo82148/actions-upload-release-asset
      id-token: write      # actions/attest-build-provenance
      attestations: write  # actions/attest-build-provenance
    name: Release build [${{ matrix.platform }}]
    if: github.repository == 'OpenCCU/OpenCCU'
    runs-on: self-hosted
    timeout-minutes: 300
    needs: release_draft
    outputs:
      build_datetime: ${{ steps.env.outputs.build_datetime }}

    strategy:
      fail-fast: false
      matrix:
        platform: ${{ fromJSON(format('[{0}]', (github.event.inputs.platforms == '' || github.event.inputs.platforms == 'all') && '"rpi3","rpi4","rpi5","odroid-c2","odroid-c4","odroid-n2","tinkerboard2","ova","oci_amd64","oci_arm64","generic-x86_64","generic-aarch64","lxc_amd64","lxc_arm64"' || github.event.inputs.platforms)) }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install Dependencies
        run: |
          if ! dpkg-query -l wget bc cpio rsync zip python3 file >/dev/null 2>&1; then
            apt update
            apt install -y --no-install-recommends wget bc cpio rsync zip python3 file
          fi
          if ! getent group | grep -q ^builder:; then groupadd -g 48 builder; fi
          if ! getent passwd | grep -q ^builder:; then useradd -m -u 1003 -g 48 -G sudo builder; fi
          if ! grep -q ^builder /etc/sudoers; then echo "builder ALL=(ALL:ALL) NOPASSWD: ALL" >>/etc/sudoers; fi
          chown -R builder:builder /home/builder

      - name: Setup Environment
        id: env
        run: |
          JLEVEL=0
          if [[ -f /sys/fs/cgroup/cpu.max ]]; then # cgroups v2
            CPU_QUOTA=$(cut -d ' ' -f1 /sys/fs/cgroup/cpu.max)
            if [[ "${CPU_QUOTA}" != "max" ]]; then
              CPU_PERIOD=$(cut -d ' ' -f2 /sys/fs/cgroup/cpu.max)
              JLEVEL=$((CPU_QUOTA / CPU_PERIOD + 1))
            fi
          elif [[ -f /sys/fs/cgroup/cpu/cpu.cfs_quota_us ]]; then # cgroups v1
            CPU_QUOTA=$(cat /sys/fs/cgroup/cpu/cpu.cfs_quota_us)
            if [[ "${CPU_QUOTA}" != "-1" ]]; then
              CPU_PERIOD=$(cat /sys/fs/cgroup/cpu/cpu.cfs_period_us)
              JLEVEL=$((CPU_QUOTA / CPU_PERIOD + 1))
            fi
          fi
          echo "JLEVEL=${JLEVEL}" >> $GITHUB_ENV
          if [[ "${{ github.event.inputs.test_workflow }}" == "true" ]]; then
            echo "FAKE_BUILD=true" >> $GITHUB_ENV
          else
            echo "FAKE_BUILD=" >> $GITHUB_ENV
          fi
          echo "build_datetime=$(date -u +'%FT%T.%3NZ')" >> $GITHUB_OUTPUT

      - name: Download ChangeLog summary artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8
        with:
          name: changelog-summary
          path: /tmp/changelog-summary

      - name: Inject ChangeLog summary into EULA files
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          summary_dir = Path("/tmp/changelog-summary")
          begin_marker = b"<!-- OPENCCU_CHANGELOG_BEGIN -->"
          end_marker   = b"<!-- OPENCCU_CHANGELOG_END -->"

          def get_fragment(eula_path: Path) -> bytes:
              """Return the locale-appropriate summary fragment, falling back to English."""
              # EULA.de, EULA.de_nightly → locale "de"; EULA.en, EULA.en_nightly → locale "en"
              suffix = eula_path.name.split(".", 1)[1] if "." in eula_path.name else "en"
              locale = suffix.split("_")[0]
              locale_file = summary_dir / f"changelog-summary.{locale}.html"
              if locale_file.exists():
                  return locale_file.read_bytes()
              # Fallback to English
              return (summary_dir / "changelog-summary.en.html").read_bytes()

          for eula in Path("release/updatepkg").glob("*/EULA.*"):
            content = eula.read_bytes()
            fragment = get_fragment(eula)
            if begin_marker in content and end_marker in content:
              # Replace only the content between the existing markers, in-place.
              start = content.index(begin_marker) + len(begin_marker)
              end   = content.index(end_marker)
              content = content[:start] + b"\n" + fragment + b"\n" + content[end:]
              eula.write_bytes(content)
            else:
              # Fallback for files that don't yet carry the markers.
              marker = b"</body>"
              if marker not in content:
                raise RuntimeError(
                  f"Could not find OPENCCU_CHANGELOG markers or </body> in {eula}"
                )
              section = begin_marker + b"\n" + fragment + b"\n" + end_marker
              eula.write_bytes(content.replace(marker, section + b"\n" + marker, 1))
          PY

      # - name: remote debug tmate session
      #   uses: mxschmitt/action-tmate@v1
      #   if: matrix.platform == 'ova'

      # major build step
      - name: Build
        timeout-minutes: 300
        run: |
          rm -rf release/OpenCCU-* buildroot-????.*
          sudo chown -R builder:builder "$GITHUB_WORKSPACE"
          sudo -H -E -u builder nice -n 19 make DATE=${{ needs.release_draft.outputs.date }} BR2_DL_DIR=/mnt/download BR2_CCACHE_DIR=/mnt/ccache/${{ matrix.platform }} BR2_JLEVEL=${{ env.JLEVEL }} PRODUCT=${{ matrix.platform }} clean-all release

      # cleanup
      - name: Cleanup
        run: |
          sudo -H -E -u builder make clean-all
          rm -rf release/*.img* buildroot-????.*

      #######################
      # release uploads
      - name: Upload release snapshot [rpi*, odroid-*, ova, generic-*]
        if: |
          !startsWith(matrix.platform, 'oci_') &&
          !startsWith(matrix.platform, 'lxc_')
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.zip
          asset_content_type: application/zip
          overwrite: true

      - name: Upload build release checksum [rpi*, ova, generic-*]
        if: |
          !startsWith(matrix.platform, 'oci_') &&
          !startsWith(matrix.platform, 'lxc_')
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.zip.sha256
          asset_content_type: text/plain
          overwrite: true

      - name: Upload build release [ccu3]
        if: |
          matrix.platform == 'rpi3'
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-ccu3.tgz
          asset_content_type: application/gzip
          overwrite: true

      - name: Upload build release checksum [ccu3]
        if: |
          matrix.platform == 'rpi3'
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-ccu3.tgz.sha256
          asset_content_type: text/plain
          overwrite: true

      - name: Upload build release [ova]
        if: |
          matrix.platform == 'ova'
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}.ova
          asset_content_type: application/gzip
          overwrite: true

      - name: Upload build release checksum [ova]
        if: |
          matrix.platform == 'ova'
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}.ova.sha256
          asset_content_type: text/plain
          overwrite: true

      - name: Upload build release [oci]
        if: |
          startsWith(matrix.platform, 'oci_')
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.tgz
          asset_content_type: application/gzip
          overwrite: true

      - name: Upload build release checksum [oci]
        if: |
          startsWith(matrix.platform, 'oci_')
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.tgz.sha256
          asset_content_type: text/plain
          overwrite: true

      - name: Upload build release [lxc]
        if: |
          startsWith(matrix.platform, 'lxc_')
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.tar.xz
          asset_content_type: application/x-xz
          overwrite: true

      - name: Upload build release checksum [lxc]
        if: |
          startsWith(matrix.platform, 'lxc_')
        uses: shogo82148/actions-upload-release-asset@aaba0f56bdbc1071f4af234d5cb16055e8a400de  # v1
        with:
          upload_url: ${{ needs.release_draft.outputs.upload_url }}
          asset_path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.tar.xz.sha256
          asset_content_type: text/plain
          overwrite: true

      ########################
      # upload build artifacts
      - name: Upload build artifact [oci]
        id: upload-oci
        if: |
          startsWith(matrix.platform, 'oci_')
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.tgz*
          name: OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.tgz
        continue-on-error: true

      - name: Attest build artifact provenance [oci]
        if: |
          startsWith(matrix.platform, 'oci_')
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4
        with:
          subject-name: OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.tgz
          subject-digest: sha256:${{ steps.upload-oci.outputs.artifact-digest }}
        continue-on-error: true

      #######################
      # manifest file artifact upload
      - name: Upload manifest artifact
        id: upload-manifest
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          path: release/OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.mf
          name: OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.mf

      - name: Attest manifest artifact provenance
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4
        with:
          subject-name: OpenCCU-${{ needs.release_draft.outputs.version }}-${{ matrix.platform }}.mf
          subject-digest: sha256:${{ steps.upload-manifest.outputs.artifact-digest }}
        continue-on-error: true

  ##########################################
  # Update checksums in release draft
  update-checksums:
    permissions:
      contents: write  # ncipollo/release-action
    name: Update checksums
    runs-on: ubuntu-24.04
    needs: [release_draft, build]

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      # download all artifact files
      - name: Download all workflow artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8

      - name: Patch release draft
        shell: bash
        run: |
          for f in */*.mf; do
            while read -r line; do
              NEEDLE=$(echo "${line}" | awk '{print $3}' | sed 's/.*-\(.*\..*\)$/\1/')
              SHACKS=$(echo "${line}" | awk '{print $2}')
              if [[ "${NEEDLE##*.}" == "ova" ]]; then
                NEEDLE="ova"
              fi
              sed -i "s/XSHA${NEEDLE}X/${SHACKS}/" release-template.md/release-template.md
            done < <(cat ${f})
          done

      - name: Update release draft
        uses: ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd  # v1.21.0
        with:
          tag: ${{ needs.release_draft.outputs.tag }}
          bodyFile: release-template.md/release-template.md
          allowUpdates: true
          draft: true
          prerelease: false
          omitNameDuringUpdate: true
          omitPrereleaseDuringUpdate: true
          token: ${{ secrets.RELEASE_USER_PAT }}

  ##########################################
  # OCI/Docker build and registry push step
  oci-multiarch-build-push:
    permissions:
      contents: write      # ncipollo/release-action
      packages: write      # docker/build-push-action
      artifact-metadata: write  # docker/build-push-action build record
      id-token: write      # actions/attest-build-provenance
      attestations: write  # actions/attest-build-provenance
    name: OCI/Docker Build+Push
    runs-on: ubuntu-24.04
    needs: [release_draft, build]
    if: github.event_name != 'pull_request' &&
      ( github.event.inputs.platforms == '' ||
        github.event.inputs.platforms == 'all' ||
        contains(github.event.inputs.platforms, 'oci_'))

    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends jq

      - name: Setup Environment
        run: |
          echo "GIT_REF=$(git symbolic-ref -q --short HEAD || git describe --tags --exact-match)" >> $GITHUB_ENV

      # download OCI platform artifacts
      - name: Download all OCI artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8
        with:
          pattern: OpenCCU-${{ needs.release_draft.outputs.version }}-oci_*.tgz
          merge-multiple: true

      - name: Extract OCI artifacts
        run: |
          rm -rf oci_build
          mkdir -p oci_build
          cd oci_build || exit 1
          shopt -s nullglob
          for f in ../OpenCCU-${{ needs.release_draft.outputs.version }}-oci_*.tgz; do
            echo "extracting ${f}..."
            DIR=$(basename "${f}" .tgz)
            mkdir -p "${DIR}"
            tar -C "${DIR}" -xf "${f}"
            LAYER=$(jq -r '.[0].Layers[0]' "${DIR}/manifest.json")
            mv -f "${DIR}/${LAYER}" "./${DIR}.tar"
            rm -rf "${DIR}" "${f}"*
          done

      - name: Build OCI tags
        shell: bash
        run: |
          BASE_TAG="ghcr.io/${GITHUB_REPOSITORY@L}"
          UNIQUE_TAG="${BASE_TAG}:${{ needs.release_draft.outputs.version }}"
          BRANCH="${GITHUB_REF##*/}"
          if [[ ${BRANCH} == 'master' ]]; then
            BRANCH_TAG="${BASE_TAG}:latest"
          else
            BRANCH_TAG="${BASE_TAG}:latest-${BRANCH}"
          fi
          echo "base_tag=${BASE_TAG}" >> $GITHUB_OUTPUT
          echo "unique_tag=${UNIQUE_TAG}" >> $GITHUB_OUTPUT
          echo "branch_tag=${BRANCH_TAG}" >> $GITHUB_OUTPUT
        id: extract_branch

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0
        with:
          use: true

      - name: Login to GitHub Container Registry
        if: github.event.inputs.test_workflow != 'true'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CR_PAT }}

      - name: Extract docker metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: ${{ steps.extract_branch.outputs.base_tag }}
          labels: |
            org.opencontainers.image.description=Alternative OS for your homematicIP CCU
            org.opencontainers.image.vendor=OpenCCU OpenSource Project
            org.opencontainers.image.authors=OpenCCU OpenSource Team
            org.opencontainers.image.version=${{ needs.release_draft.outputs.version }}
            org.opencontainers.image.url=https://openccu.de
            org.opencontainers.image.created=${{ needs.build.outputs.build_datetime }}
          annotations: |
            org.opencontainers.image.description=Alternative OS for your homematicIP CCU
            org.opencontainers.image.vendor=OpenCCU OpenSource Project
            org.opencontainers.image.authors=OpenCCU OpenSource Team
            org.opencontainers.image.version=${{ needs.release_draft.outputs.version }}
            org.opencontainers.image.url=https://openccu.de
            org.opencontainers.image.created=${{ needs.build.outputs.build_datetime }}
        env:
          DOCKER_METADATA_ANNOTATIONS_LEVELS: manifest,index

      - name: Build and push container image
        if: github.event.inputs.test_workflow != 'true'
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        id: docker_build
        with:
          context: oci_build
          file: buildroot-external/board/oci/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            tar_prefix=OpenCCU-${{ needs.release_draft.outputs.version }}-oci_
          labels: |
            org.opencontainers.image.title=OpenCCU
            org.opencontainers.image.description=Alternative OS for your homematicIP CCU
            org.opencontainers.image.vendor=OpenCCU OpenSource Project
            org.opencontainers.image.authors=OpenCCU OpenSource Team
            org.opencontainers.image.licenses=Apache-2.0
            org.opencontainers.image.url=https://openccu.de
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.documentation=https://github.com/${{ github.repository }}/wiki
            org.opencontainers.image.created=${{ needs.build.outputs.build_datetime }}
            org.opencontainers.image.ref.name=${{ env.GIT_REF }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ needs.release_draft.outputs.version }}
            io.hass.name=OpenCCU
            io.hass.description=HomeMatic/homematicIP CCU central based on OpenCCU
            io.hass.url=https://github.com/${{ github.repository }}/tree/master/home-assistant-addon
            io.hass.version=${{ needs.release_draft.outputs.version }}
            io.hass.type=addon
            io.hass.arch=aarch64|amd64
          tags: |
            ${{ steps.extract_branch.outputs.unique_tag }}
            ${{ steps.extract_branch.outputs.branch_tag }}
          annotations: |
            ${{ steps.meta.outputs.annotations }}

      - name: Container image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

      - name: Attest container provenance
        if: github.event.inputs.test_workflow != 'true'
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4
        id: attest
        with:
          subject-name: ${{ steps.extract_branch.outputs.base_tag }}
          subject-digest: ${{ steps.docker_build.outputs.digest }}
          push-to-registry: true

      - name: Cleanup oci_build dir
        run: |
          rm -rf oci_build

  ##########################################
  # Publish new version to public
  publish-build:
    permissions:
      contents: write  # github-actions[bot] repo write access
    name: Publish build
    runs-on: ubuntu-24.04
    needs: [release_draft, build, oci-multiarch-build-push]

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: master
          persist-credentials: true
          ssh-key: ${{ secrets.DEPLOY_KEY }}
          fetch-depth: 0

      - name: Bump HomeAssistant add-on version
        run: |
          sed -i "s/^\(version:\)\(.*\)/\1 ${{ needs.release_draft.outputs.version }}/" home-assistant-addon/config.yaml

      # download rpi manifest files
      - name: Download rpi3 manifest
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8
        with:
          name: OpenCCU-${{ needs.release_draft.outputs.version }}-rpi3.mf
      - name: Download rpi4 manifest
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8
        with:
          name: OpenCCU-${{ needs.release_draft.outputs.version }}-rpi4.mf
      - name: Download rpi5 manifest
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8
        with:
          name: OpenCCU-${{ needs.release_draft.outputs.version }}-rpi5.mf

      - name: Bump rpi-imager version
        run: |
          release/rpi-imager-update.sh release/rpi-imager.json \
            OpenCCU-${{ needs.release_draft.outputs.version }}-rpi3.mf \
            OpenCCU-${{ needs.release_draft.outputs.version }}-rpi4.mf \
            OpenCCU-${{ needs.release_draft.outputs.version }}-rpi5.mf

      - name: Bump LATEST-VERSION.js
        run: |
          echo "homematic.com.setLatestVersion('${{ needs.release_draft.outputs.version }}', 'HM-RASPBERRYMATIC');" >release/LATEST-VERSION.js

      - name: Bump helm Chart.yaml
        shell: bash
        run: |
          VERSION=${{ needs.release_draft.outputs.version }}
          sed -i "s/^\(version:\)\(.*\)/\1 ${VERSION::-9}/" helm/openccu/Chart.yaml
          sed -i "s/^\(appVersion:\)\(.*\)/\1 ${VERSION}/" helm/openccu/Chart.yaml
          sed -i "s|- Release of .*|- Release of ${VERSION} (https://github.com/OpenCCU/OpenCCU/releases/tag/${VERSION})|" helm/openccu/Chart.yaml

      - name: Show git diffs
        shell: bash
        run: |
          git status
          git diff

      - name: Commit changes
        if: github.event.inputs.test_workflow != 'true'
        run: |
          git config user.name "github-actions"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash
          git diff --quiet || git commit -a -m "release bump [${{ needs.release_draft.outputs.version }}]"
          git push origin HEAD:master

  helm:
    permissions:
      contents: write  # stefanprodan/helm-gh-pages
    name: Build K8s Helm chart
    runs-on: ubuntu-24.04
    needs: [release_draft, oci-multiarch-build-push, publish-build]

    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0

      - name: Publish Helm chart
        if: github.event.inputs.test_workflow != 'true'
        uses: stefanprodan/helm-gh-pages@0ad2bb377311d61ac04ad9eb6f252fb68e207260  # v1.7.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          charts_dir: helm
          chart_version: ${{ needs.release_draft.outputs.occu_version }}
          app_version: ${{ needs.release_draft.outputs.version }}
