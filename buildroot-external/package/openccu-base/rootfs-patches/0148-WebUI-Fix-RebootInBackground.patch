--- a/www/api/methods/safemode/enter.tcl
+++ b/www/api/methods/safemode/enter.tcl
@@ -18,6 +18,6 @@
 
 # Neustart
 rega system.Save()
-exec /sbin/reboot
+exec /sbin/reboot -d 2 2>/dev/null >/dev/null &
 
-jsonrpc_response true
+jsonrpc_response true
--- a/www/config/cp_maintenance.cgi
+++ b/www/config/cp_maintenance.cgi
@@ -1270,8 +1270,7 @@
     exec /bin/kill -SIGQUIT 1
   } else {
     exec touch /usr/local/.recoveryMode
-    exec sleep 5
-    exec /sbin/reboot
+    exec /sbin/reboot -d 2 2>/dev/null >/dev/null &
   }
 
 }
@@ -1282,8 +1281,7 @@
   catch { exec lcdtool {Saving   Data...  } }
   rega system.Save()
   catch { exec lcdtool {Reboot...       } }
-  exec sleep 5
-  exec /sbin/reboot
+  exec /sbin/reboot -d 2 2>/dev/null >/dev/null &
 }
 proc action_shutdown {} {
   puts ""
@@ -1291,9 +1289,8 @@
   catch { exec lcdtool {Saving   Data...  } }
   rega system.Save()
   catch { exec lcdtool {Shutdown...       } }
-  exec sleep 5
   catch { exec touch /tmp/shutdown }
-  exec /sbin/poweroff
+  exec /sbin/poweroff -d 2 2>/dev/null >/dev/null &
 }
 
 proc get_logserver {} {
--- a/www/config/cp_security.cgi
+++ b/www/config/cp_security.cgi
@@ -1564,7 +1564,7 @@
 
 proc action_reboot {} {
   puts ""
-  exec /sbin/reboot
+  exec /sbin/reboot -d 2 2>/dev/null >/dev/null &
 }
 
 proc _version_compare { v1 v2 } {
