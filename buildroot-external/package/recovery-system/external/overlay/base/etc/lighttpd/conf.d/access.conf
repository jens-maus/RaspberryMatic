$HTTP["url"] =~ "^/etc" {
        url.access-deny = ("")
}

$HTTP["url"] =~ "^/bin" {
        url.access-deny = ("")
}

$HTTP["url"] =~ "^/boot" {
        url.access-deny = ("")
}

$HTTP["url"] =~ "^/usr" {
        url.access-deny = ("")
}

$HTTP["url"] =~ "^/usr/bin" {
        url.access-allow = ("")
}
